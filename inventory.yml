all:
  children:
    master:
      hosts:
        master-jenkins:
          ansible_host: "{{ terraform_output.master_ip }}"
    nexus:
      hosts:
        nexus:
          ansible_host: "{{ terraform_output.nexus_ip }}"
    build_node:
      hosts:
        build-node:
          ansible_host: "{{ terraform_output.build_nodes_ips }}"
    prod_node:
      hosts:
        prod-node:
          ansible_host: "{{ terraform_output.prod_node_ip }}"

